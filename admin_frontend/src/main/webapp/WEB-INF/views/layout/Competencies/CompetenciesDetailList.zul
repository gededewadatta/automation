<zk>

    <window width="auto" border="none"
            apply="admin.fe.controller.Maintenance.Competencies.CompetenciesDetailListController">

        <div>
            <div>
                <grid>
                    <rows>
                        <row>
                            <label value = "Department:" />
                            <hbox>
                                <hlayout>
                                    <textbox id="idDepartment" value="${win$composer.inventoryItem.createdDate}"
                                             constraint="no empty" />
                                </hlayout>
                                <div>
                                    <button id="btnDepartement"/>
                                </div>
                            </hbox>
                        </row>
                        <row>
                            <label value = "Grade:" />
                            <hbox>
                                <hlayout>
                                    <textbox id="idGrade" value="${win$composer.inventoryItem.createdDate}"
                                             constraint="no empty" />
                                </hlayout>
                                <div>
                                    <button id="btnGrade"/>
                                </div>
                            </hbox>


                        </row>
                        <row>
                            <label value = "Sub Grade:" />
                            <hbox>
                                <hlayout>
                                    <textbox id="idSubGrade" value="${win$composer.inventoryItem.createdDate}"
                                             constraint="no empty" />
                                </hlayout>
                                <div>
                                    <button id="btnSubGrade"/>
                                </div>
                            </hbox>

                        </row>

                        <row>
                            <label value = "Competencies:" />
                            <hbox>
                                <hlayout>
                                    <textbox id="idCompetencies" value="${win$composer.inventoryItem.createdDate}"
                                             constraint="no empty" />
                                </hlayout>
                            </hbox>

                        </row>
                    </rows>
                </grid>
            </div>


        </div>

    </window>
</zk>