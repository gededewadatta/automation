<zk>

    <window width="auto" border="none"
            apply="admin.fe.controller.Maintenance.Departement.DepartementDetailController">

        <div>
            <grid>
                <rows>
                    <row>
                        <label value = "Division:" />
                        <hbox>
                            <hlayout>
                                <textbox id="idDivision" value="${win$composer.inventoryItem.createdDate}"
                                         constraint="no empty" />
                            </hlayout>
                            <div>
                                <button id ="btnDivSeearch"/>
                            </div>
                        </hbox>
                    </row>
                    <row>
                        <label value = "Departement:" />
                        <hbox>
                            <hlayout>
                                <textbox id="idDepartement" value="${win$composer.inventoryItem.createdDate}"
                                         constraint="no empty" />
                            </hlayout>
                        </hbox>


                    </row>
                    <row>
                        <label value = "Departement Name:" />
                        <hbox>
                            <hlayout>
                                <textbox id="nameDepartement" value="${win$composer.inventoryItem.createdDate}"
                                         constraint="no empty" />
                            </hlayout>
                        </hbox>

                    </row>

                </rows>
            </grid>
            <div align="right" style="margin-top:30px">

                <button id="submitButton" label="Submit" style="float::right"/>
                <button id="clearButton" label="Clear" style="float::right"/>

            </div>

        </div>

    </window>
</zk>